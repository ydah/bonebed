#!/usr/bin/env bash
set -euo pipefail

docker image inspect bonebed-dev >/dev/null 2>&1 || docker build -f Dockerfile.dev -t bonebed-dev .
exec docker run --rm --security-opt seccomp=unconfined \
  -v "$PWD:/app" -v bonebed-bundle:/usr/local/bundle -w /app bonebed-dev "$@"
